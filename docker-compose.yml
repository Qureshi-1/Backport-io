<<<<<<< HEAD
version: '3.8'

services:
  backend_gateway:
    build: ./backend
    container_name: backpack_gateway
    ports:
      - "8080:8080"
    environment:
      - TARGET_BACKEND_URL=http://dummy_backend:3001
      - DB_PATH=/data/backpack.db
    command: uvicorn main:app --host 0.0.0.0 --port 8080
    restart: always
    volumes:
      - backend_data:/data
    networks:
      - backpack_network
    depends_on:
      - dummy_backend

  dummy_backend:
    build: ./backend
    container_name: backpack_dummy
    ports:
      - "3001:3001"
    command: uvicorn dummy_backend:app --host 0.0.0.0 --port 3001
    restart: always
    networks:
      - backpack_network

  frontend_dashboard:
    build: ./frontend
    container_name: backpack_dashboard
    ports:
      - "3000:3000"
    restart: always
    networks:
      - backpack_network
    depends_on:
      - backend_gateway

networks:
  backpack_network:
    driver: bridge

volumes:
  backend_data:
=======
# ── Backport — Full Stack Docker Compose ──────────────────────────────────
# Usage: docker compose up -d
# Includes: Backend (FastAPI), PostgreSQL, Redis

services:
  # ── PostgreSQL Database ─────────────────────────────────────────────────
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: backport
      POSTGRES_PASSWORD: backport_dev_password
      POSTGRES_DB: backport
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U backport -d backport"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ── Redis (Caching + Rate Limiting) ────────────────────────────────────
  redis:
    image: redis:7-alpine
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ── Backend API ────────────────────────────────────────────────────────
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql://backport:backport_dev_password@db:5432/backport
      REDIS_URL: redis://redis:6379/0
      ENVIRONMENT: production
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

volumes:
  pg_data:
  redis_data:
>>>>>>> 369eadd36bd1a259f5b95fb908ea824a3484f6cc
