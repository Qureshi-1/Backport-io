[pytest]
testpaths = tests
asyncio_mode = auto

# Suppress noisy warnings
filterwarnings =
    ignore::DeprecationWarning
    ignore:.*MovedIn20Warning.*
    ignore:.*SAWarning.*
    ignore::UserWarning
